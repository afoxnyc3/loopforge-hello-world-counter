# loopforge-hello-world-counter
A single-page counter app. One HTML file, one CSS file, one JS file. Click a button to increment a number. Dark theme, centered layout. No frameworks, no build tools, pure vanilla HTML/CSS/JS.
